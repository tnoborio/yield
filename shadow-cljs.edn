;; Development only - used by Calva Jack-in for CLJS REPL.
;; Production builds are handled by duct.module/cljs.
{:deps true
 :builds
 {:app {:target :browser
        :output-dir "target/cljs"
        :asset-path "/cljs"
        :modules {:app {:entries [yield.app]
                        :init-fn yield.app/init}}}}}

services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_DB: yield_dev
      POSTGRES_USER: yield
      POSTGRES_PASSWORD: yield
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
